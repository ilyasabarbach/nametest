import { tokens } from "./tokens";

export const theme = {
  background: [tokens.colors.deep, tokens.colors.navy],
  card: "rgba(14, 21, 46, 0.84)",
  text: tokens.colors.ink,
  accent: tokens.colors.gold
};
